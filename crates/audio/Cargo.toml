[package]
name = "audio"
version = "0.1.0"
edition = "2021"

[dependencies]
cpal      = "0.15"
log       = "0.4"
thiserror = "1"
symphonia = { version = "0.5", default-features = false, features = ["mpa", "aac", "isomp4"] }
